import React, { Component } from 'react';
import {
	View, Text
} from 'react-native';



var deviceWidth = Dimensions.get('window').width;
var deviceHeight = Dimensions.get('window').height;

export default class ViewCommentPost extends Component {
	render() {
		return (
			null
		);
	}
}
